server {
    listen 80;
    
    root /usr/share/nginx/html;
    index IndexMain/index.html;
    
    location = / {
        return 301 /IndexMain/index.html;
    }
    
    location / {
        try_files $uri $uri/ =404;
    }
}